# Brainstorm mockups (visual companion)
.superpowers/

# Dependencies
node_modules/

# Next.js / build
.next/
.open-next/
out/
dist/

# Cloudflare
.wrangler/
.dev.vars
# Deploy-time config with the real D1 database_id injected (see scripts/deploy.mjs).
# NEVER commit this — it contains the real id; the tracked wrangler.toml has a placeholder.
wrangler.generated.toml

# Env
.env
.env.local

# Generated / local data (do not commit)
migrations/seed.sql
/output.csv

# OS / editor
.DS_Store
*.log

# Next.js (create-next-app additions)
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/build
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.env*
.vercel
*.tsbuildinfo
next-env.d.ts
